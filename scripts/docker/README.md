This directory contains docker-related helper scripts moved from the legacy location.

Make the scripts executable before running:

```bash
chmod +x scripts/docker/*.sh
```

Files:
- docker-demo.sh
- update-docker-setup.sh
- test-docker-setup.sh
